/* eslint-disable no-console */
// Seeder is not used for SeparateUserService or ExternalProvider
// User management is handled by external service

export class Seeder {

    public init = async (): Promise<void> => {
        // No seeding required - handled by external user service
    };

}
